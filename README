Requirements:

	# sudo apt-get install cowbuilder python-configobj

Create an instance:

	#sudo ./automate-tool --instance test --create

	edit configuration files in instances/test/

	#sudo ./automate-tool --instance test --create-pbuilder

	set up your FTP server with anonymous upload to instances/test/upload

	create a cron job in /etc/cron.d/ (see cron.sample)
